services:
  api:
    container_name: forum-api
    env_file:
      - .env
      - .env.local
    extra_hosts:
      - "host.docker.internal:host-gateway"
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
      CHOKIDAR_USEPOLLING: "true"
    # Anonymous /app/node_modules volume goes stale when package.json gains deps;
    # reinstall on start so Nest can compile without requiring --build every time.
    command: sh -c "bun install && bun run start:dev"
    networks:
      - forum.test

networks:
  forum.test:
    name: forum.test
    external: true
